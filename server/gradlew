#!/bin/sh
# Gradle wrapper script (Unix)
# Requires gradle-wrapper.jar - download via: gradle wrapper --gradle-version 8.8

SCRIPT_DIR=$(dirname "$0")
exec java -jar "$SCRIPT_DIR/gradle/wrapper/gradle-wrapper.jar" "$@"
